{
	"$schema": "http://json.schemastore.org/package",
	"private": true,
	"name": "macro-ai",
	"type": "module",
	"scripts": {
		"commit": "cz",
		"build": "turbo run build",
		"build:types": "turbo run build --filter=@repo/types-macro-ai-api",
		"dev": "turbo run dev --env-mode=loose",
		"dev:types": "turbo run dev --filter=@repo/types-macro-ai-api",
		"clean:types": "turbo run clean --filter=@repo/types-macro-ai-api",
		"generate:types": "turbo run generate --filter=@repo/types-macro-ai-api",
		"lint": "turbo run lint",
		"format": "prettier --check .",
		"format:fix": "prettier --write .",
		"ui": "pnpm --filter @repo/ui ui",
		"test": "turbo run test",
		"prepare": "husky"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"@vitest/ui": "^3.1.2",
		"commitizen": "^4.3.1",
		"cz-conventional-changelog": "^3.3.0",
		"eslint": "catalog:",
		"husky": "^9.1.7",
		"prettier": "catalog:",
		"tsx": "^4.19.4",
		"turbo": "^2.5.2",
		"vitest": "^3.1.2"
	},
	"packageManager": "pnpm@10.9.0",
	"engines": {
		"node": ">=20"
	},
	"config": {
		"commitizen": {
			"path": "cz-conventional-changelog"
		}
	}
}
