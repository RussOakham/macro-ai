# API Configuration
VITE_API_URL=http://localhost:3040/api # Must be a valid URL
VITE_API_KEY= # Must be at least 32 characters - Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
