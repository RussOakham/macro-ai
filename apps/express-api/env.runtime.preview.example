# Runtime Environment Variables for Preview Deployments
# This file contains runtime environment variables injected by CDK at deployment time
# This file is automatically generated and should not be manually edited

# Preview Deployment Configuration
NODE_ENV=production
APP_ENV=pr-123
SERVER_PORT=3040

# Deployment Information
PR_NUMBER=123
BRANCH_NAME=feature/my-feature
CORS_ALLOWED_ORIGINS=https://preview-pr-123.your-domain.com
CUSTOM_DOMAIN_NAME=preview-pr-123.your-domain.com

# API Configuration (Resolved from Parameter Store at CDK synthesis time)
API_KEY=${API_KEY}
COOKIE_ENCRYPTION_KEY=${COOKIE_ENCRYPTION_KEY}

# AWS Cognito Configuration (Resolved from Parameter Store at CDK synthesis time)
AWS_COGNITO_REGION=${AWS_COGNITO_REGION}
AWS_COGNITO_USER_POOL_ID=${AWS_COGNITO_USER_POOL_ID}
AWS_COGNITO_USER_POOL_CLIENT_ID=${AWS_COGNITO_USER_POOL_CLIENT_ID}
AWS_COGNITO_USER_POOL_SECRET_KEY=${AWS_COGNITO_USER_POOL_SECRET_KEY}
AWS_COGNITO_ACCESS_KEY=${AWS_COGNITO_ACCESS_KEY}
AWS_COGNITO_SECRET_KEY=${AWS_COGNITO_SECRET_KEY}

# Database Configuration (Resolved from Parameter Store at CDK synthesis time)
RELATIONAL_DATABASE_URL=${RELATIONAL_DATABASE_URL}
REDIS_URL=${REDIS_URL}

# OpenAI Configuration (Resolved from Parameter Store at CDK synthesis time)
OPENAI_API_KEY=${OPENAI_API_KEY}

# Rate Limiting Configuration (Resolved from Parameter Store at CDK synthesis time)
RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS}
RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS}
AUTH_RATE_LIMIT_WINDOW_MS=${AUTH_RATE_LIMIT_WINDOW_MS}
AUTH_RATE_LIMIT_MAX_REQUESTS=${AUTH_RATE_LIMIT_MAX_REQUESTS}
API_RATE_LIMIT_WINDOW_MS=${API_RATE_LIMIT_WINDOW_MS}
API_RATE_LIMIT_MAX_REQUESTS=${API_RATE_LIMIT_MAX_REQUESTS}

# Optional Configuration (Resolved from Parameter Store at CDK synthesis time)
REDIS_URL=${REDIS_URL}
COOKIE_DOMAIN=${COOKIE_DOMAIN}
AWS_COGNITO_REFRESH_TOKEN_EXPIRY=${AWS_COGNITO_REFRESH_TOKEN_EXPIRY}
