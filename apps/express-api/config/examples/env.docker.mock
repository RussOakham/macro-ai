# Mock Environment Variables for Docker Testing
# This file contains dummy values that allow the container to start
# DO NOT USE IN PRODUCTION - These are mock values for testing only

# Development Configuration
NODE_ENV=development
APP_ENV=development
SERVER_PORT=3040
# For Docker builds only; do not set in production
SKIP_ENV_VALIDATION=true

# API Configuration - Mock values with proper length requirements
API_KEY=mock-api-key-for-docker-testing-32-chars-minimum-length-required
COOKIE_ENCRYPTION_KEY=mock-cookie-encryption-key-for-docker-testing-32-chars-minimum

# AWS Cognito Configuration - Mock values
AWS_COGNITO_REGION=us-east-1
AWS_COGNITO_USER_POOL_ID=us-east-1_MockPool123
AWS_COGNITO_USER_POOL_CLIENT_ID=mock123client456id789
AWS_COGNITO_USER_POOL_SECRET_KEY=mock-secret-key-for-docker-testing-purposes

# Database Configuration - Mock values (won't actually connect)
RELATIONAL_DATABASE_URL=postgresql://mockuser:mockpass@localhost:5432/mock_db
REDIS_URL=redis://localhost:6379

# OpenAI Configuration - Mock API key
OPENAI_API_KEY=sk-mock-openai-api-key-for-docker-testing-purposes-only

# Rate Limiting Configuration - Relaxed for testing
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
AUTH_RATE_LIMIT_WINDOW_MS=3600000
AUTH_RATE_LIMIT_MAX_REQUESTS=50
API_RATE_LIMIT_WINDOW_MS=60000
API_RATE_LIMIT_MAX_REQUESTS=600

# Optional Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
COOKIE_DOMAIN=localhost
AWS_COGNITO_REFRESH_TOKEN_EXPIRY=30
