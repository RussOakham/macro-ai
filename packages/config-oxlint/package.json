{
	"$schema": "http://json.schemastore.org/package",
	"name": "@repo/config-oxlint",
	"license": "MIT",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"engines": {
		"node": ">=20.0.0",
		"pnpm": ">=9.0.0"
	},
	"files": [
		"./base.json"
	],
	"exports": {
		"./base.json": "./base.json"
	},
	"scripts": {
		"format": "prettier --check --cache .",
		"format:fix": "prettier --write --cache .",
		"lint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:fix": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:ci": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:oxlint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:fix:oxlint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:ci:oxlint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:eslint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:fix:eslint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'",
		"lint:ci:eslint": "echo 'No JavaScript/TypeScript files to lint in config-oxlint'"
	},
	"devDependencies": {
		"prettier": "catalog:"
	},
	"publishConfig": {
		"access": "public"
	}
}
