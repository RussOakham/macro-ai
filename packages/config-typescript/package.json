{
	"$schema": "http://json.schemastore.org/package",
	"name": "@repo/config-typescript",
	"license": "MIT",
	"private": "true",
	"version": "0.0.0",
	"type": "module",
	"engines": {
		"node": ">=20.0.0",
		"pnpm": ">=9.0.0"
	},
	"scripts": {
		"lint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:fix": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:ci": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:oxlint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:fix:oxlint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:ci:oxlint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:eslint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:fix:eslint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'",
		"lint:ci:eslint": "echo 'No JavaScript/TypeScript files to lint in config-typescript'"
	},
	"dependencies": {
		"@total-typescript/tsconfig": "^1.0.4"
	},
	"devDependencies": {
		"@repo/config-oxlint": "workspace:*"
	},
	"publishConfig": {
		"access": "public"
	}
}
